/**
 * AnalyticsLoader - GTM-managed analytics
 * 
 * Analytics is now managed entirely via Google Tag Manager (GTM-NCS5BSN9)
 * with consent defaults and CookieFirst integration in index.html.
 * 
 * This component is kept as a placeholder for any future React-specific
 * analytics needs (e.g., route change tracking via React Router).
 */
export function AnalyticsLoader() {
  // GTM handles all analytics via index.html
  // No React-based GA4 loading needed
  return null;
}
